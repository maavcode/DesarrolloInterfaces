<UserControl x:Class="playground_.net_BasicThings.Frontend.UserControls.ListaUsuariosUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:playground_.net_BasicThings.Frontend.UserControls"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="DiagListaModelosArticulos_Loaded">
    <Grid>
        <DockPanel>
            <Grid DockPanel.Dock="Top">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Lista Modelos Articulos"/>
                    <!-- Criterios -->
                    <StackPanel Orientation="Horizontal"
                            Grid.Row="1">
                        <TextBlock Text="FILTROS"
                               Margin="10" VerticalAlignment="Center"
                               Foreground="Black"/>

                    </StackPanel>

                </Grid>
            </Grid>
            <Grid>
                <DataGrid 
    SelectedItem="{Binding usuario}"
    ItemsSource="{Binding listaUsuarios}"
    AutoGenerateColumns="False"
    CanUserAddRows="False"
    ColumnWidth="*">

                    <DataGrid.Columns>

                        <DataGridTemplateColumn Header="Nombre completo">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock>
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="{}{0} {1} {2}">
                                                <Binding Path="Nombre"/>
                                                <Binding Path="Apellido1"/>
                                                <Binding Path="Apellido2"/>
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>


                        <!-- Username -->
                        <DataGridTextColumn Header="Username"
                            Binding="{Binding Username}" />

                        <!-- Información laboral (tipo, rol, grupo, departamento) -->
                        <DataGridTemplateColumn Header="Información laboral">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Tipo}" />
                                        <TextBlock Text="{Binding Rol}" />
                                        <TextBlock Text="{Binding Grupo}" />
                                        <TextBlock Text="{Binding Departamento}" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Domicilio -->
                        <DataGridTemplateColumn Header="Domicilio">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Domicilio}" />

                                        <TextBlock>
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="{}{0} ({1})">
                                                    <Binding Path="Poblacion"/>
                                                    <Binding Path="Codpostal"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>



                        <!-- Email -->
                        <DataGridTextColumn Header="Email"
                            Binding="{Binding Email}" />

                        <!-- Teléfono -->
                        <DataGridTextColumn Header="Teléfono"
                            Binding="{Binding Telefono}" />

                    </DataGrid.Columns>

                    <!-- Menú contextual -->
                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Editar modelo artículo"
                                      Click="editaUsuario_Click"/>

                            <MenuItem Header="Eliminar modelo artículo"
                      x:Name="eliminarModeloArticulo"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>

                </DataGrid>

            </Grid>
        </DockPanel>
    </Grid>
</UserControl>