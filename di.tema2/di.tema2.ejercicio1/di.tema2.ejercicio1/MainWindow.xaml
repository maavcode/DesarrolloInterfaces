<Window x:Class="di.tema2.ejercicio1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:di.tema2.ejercicio1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <DockPanel>
        <!-- Parte Superior -->
        <Grid DockPanel.Dock="Top" Background="Blue">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <Button Content="Current" Margin="10" Width="100" Height="40"></Button>
            
            <Button Content="Projected" Margin="10" Width="100"
                    Height="40" HorizontalAlignment="Left" Grid.Column="1"/>
            
            <Button Grid.Column="1" Width="40" Height="40" 
                    HorizontalAlignment="Right" Margin="10">
                <Image Source="/interrogante.png" Height="24"/>
            </Button>
            
        </Grid>
        <!-- Parte Izquierda -->
        <StackPanel DockPanel.Dock="Left">
            <TextBlock Text="Data" Margin="10" FontWeight="Bold" Width="100"/>
            <TextBlock Text="Sales" Margin="20,0,10,10"/>
            <TextBlock Text="Marketing" Margin="20,0,10,10"/>
            <TextBlock Text="Distribution" Margin="20,0,10,10"/>
            <TextBlock Text="Costs" Margin="20,0,10,10"/>
        </StackPanel>
        <!-- Parte Derecha -->
        <WrapPanel DockPanel.Dock="Right" Width="120">
            <Button Margin="10" Background="Transparent" Padding="5">
                <Image Source="/barChart.png" Height="24" />
            </Button>

            <Button Margin="10" Background="Transparent" Padding="5">
                <Image Source="/lineChart.png" Height="24" />
            </Button>

            <Button Margin="10" Background="Transparent" Padding="5">
                <Image Source="/barChart2.png" Height="24" />
            </Button>

            <Button Margin="10" Background="Transparent" Padding="5">
                <Image Source="/pieChart.png" Height="24" />
            </Button>

            <Button Margin="10" Background="Transparent" Padding="5">
                <Image Source="/location.png" Height="24" />
            </Button>

            <Button Margin="10" Background="Transparent" Padding="5">
                <Image Source="/diagram.png" Height="24" />
            </Button>
        </WrapPanel>
        <!-- Parte Central -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Image Source="/home.png" Grid.Row="0" Margin="10" Height="42" Width="42" Grid.RowSpan="2"/>
            <TextBlock Text="Sales: Current Year" Margin="10, 10, 0, 5" 
                       Grid.Row="0" Grid.Column="1" FontSize="16" FontWeight="Bold"/>
            
            <TextBlock Text="Goods and Services" Margin="10, 0, 10, 0" 
                       Grid.Row="1" Grid.Column="1"/>

            <TextBlock Text="Services 20%" Grid.Column="2" Grid.Row="2" Margin="10, 0, 10, 0"/>

            <Image Source="/pieChart3D.png" Grid.Column="1" Grid.Row="2" Stretch="Uniform"/>

            <TextBlock Text="Goods 80%" Grid.Column="0" Grid.Row="3" />

            <Button Content="Save" Width="80"
                    Margin="10" Grid.Column="1" Grid.Row="4" 
                    HorizontalAlignment="Right"/>

            <Button Content="Cancel" Width="80" Margin="10" 
                    Grid.Column="2" Grid.Row="4" />
            
        </Grid>
        <!-- Parte Inferior -->
    </DockPanel>
</Window>
